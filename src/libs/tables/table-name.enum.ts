export enum TableName {
  users = 'users',
  profiles = 'profiles',
  paymentMethods = 'payment-methods',
  routes = 'routes',
  participants = 'participants',
  orders = 'orders',
  transports = 'transports',
}
